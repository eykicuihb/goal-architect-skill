/goal Achieve the requested performance target (p95 latency is below 120ms) for the relevant system while preserving correctness, verified by benchmark output showing the target metric is met; explicit acceptance checklist; final evidence-backed report, while preserving Do not regress correctness tests; Do not regress correctness tests or public behavior. Use only current repository; relevant service/module; benchmark fixtures; related tests. Between iterations, record what was inspected or changed, the evidence observed, and the next lowest-risk action. If blocked or no valid paths remain, stop with attempted paths, gathered evidence, blocker, and next input needed.
